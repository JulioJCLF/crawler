name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        # O frontend (Next.js 16) exige Node >= 20.9 (engines em next/package.json).
        # 18.x nunca passaria aqui — sempre falhava com erro de binding nativo
        # do PostCSS/Tailwind, sem relação com o código do projeto.
        node-version: [20.x, 22.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install dependencies (backend)
        run: cd backend && npm ci

      - name: Install dependencies (frontend)
        run: cd frontend && npm ci

      - name: Run unit tests (backend)
        run: cd backend && npm test

      - name: Run unit tests (frontend)
        run: cd frontend && npm test

      - name: Verify build (Type check)
        run: cd backend && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit
